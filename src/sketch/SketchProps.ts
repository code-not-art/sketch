import { Canvas, Random } from '@code-not-art/core';
import Palette from './Palette';

type SketchProps = {
  canvas: Canvas;
  palette: Palette;
  rng: Random;
};

export default SketchProps;
